apply plugin: 'java-library'

dependencies {
    api project(':microconfig:microconfig-api')

    implementation project(':microconfig:utils'),
            lib.spring_el,
            lib.gson,
            lib.snakeyaml
}