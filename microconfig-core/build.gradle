apply plugin: 'java-library'

dependencies {
    api project(':microconfig-api')

    implementation project(':utils'),
            project(':microconfig-templates:mustache'),
            lib.spring_el,
            lib.gson,
            lib.snakeyaml
}